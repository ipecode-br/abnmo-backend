NODE_ENV=test

# API Configuration
API_PORT=3334
APP_URL=http://localhost:3334
APP_ENVIRONMENT=local

# Secrets (use test values)
COOKIE_DOMAIN=localhost
COOKIE_SECRET=test-cookie-secret-key-for-testing-only
JWT_SECRET=test-jwt-secret-key-for-testing-only

# Database Configuration (Same database, separate schema for tests)
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=abnmo_dev
DB_SCHEMA=abnmo_test
DB_USERNAME=root
DB_PASSWORD=abnmo_root_password
DB_ROOT_PASSWORD=abnmo_root_password

# AWS Configuration (Test values)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=test-access-key
AWS_SECRET_ACCESS_KEY=test-secret-key
AWS_SES_FROM_EMAIL=test@example.com